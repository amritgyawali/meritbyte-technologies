// Shared by the popup (shown to the visitor) and /api/subscribe (stored with
// the row), so the consent saved in Supabase is exactly the text on screen.
// Change the version whenever the wording changes.

export const CONSENT_VERSION = "subscribe-2026-09-25";
export const CONSENT_TEXT =
  "Yes, I want to subscribe. Meritbyte Technologies may email my business about " +
  "websites and apps. I can unsubscribe at any time.";
